% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evolucion-temporal.R
\name{evolucion_temporal}
\alias{evolucion_temporal}
\title{Evolución temporal de actividad por email (pre vs long)}
\usage{
evolucion_temporal(
  pre,
  long,
  email_col = "email",
  date_col = "date",
  nivel = c("semana", "mes", "dia"),
  desde = NULL,
  hasta = NULL,
  incluir_total = TRUE
)
}
\arguments{
\item{pre}{Data frame con columnas de email y fecha.}

\item{long}{Data frame con columnas de email y fecha.}

\item{email_col}{Nombre de la columna con el email (en ambos data frames). Por defecto "email".}

\item{date_col}{Nombre de la columna con la fecha (en ambos data frames). Por defecto "date".}

\item{nivel}{Granularidad temporal: "dia", "semana" o "mes". Por defecto "semana".}

\item{desde}{Fecha mínima (Date, POSIXct o cadena "YYYY-MM-DD"). Si \code{NULL}, usa el mínimo observado.}

\item{hasta}{Fecha máxima (Date, POSIXct o cadena "YYYY-MM-DD"). Si \code{NULL}, usa el máximo observado.}

\item{incluir_total}{Si \code{TRUE}, añade columnas agregadas totales además de las de cada fuente.}
}
\value{
Un tibble con una fila por periodo y columnas:
\itemize{
\item \code{periodo_inicio}, \code{periodo_fin}
\item \code{eventos_pre}, \code{personas_pre}
\item \code{eventos_long}, \code{personas_long}
\item \code{eventos_total}, \code{personas_total}
\item \code{nuevos_total}, \code{recurrentes_total}
\item \code{acumulado_personas} (distintos global acumulado)
}
}
\description{
Une datos "pre" y "long" por \code{email} y \code{date} y calcula, por periodo,
eventos totales, personas únicas, nuevas (primera vez) y recurrentes,
con desglose por fuente (pre/long) y totales.
}
\examples{
# evolucion_temporal(pre_df, long_df, nivel = "mes", desde = "2023-01-01")

}
