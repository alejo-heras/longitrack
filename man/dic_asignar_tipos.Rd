% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3-dic-asignar-tipos.R
\name{dic_asignar_tipos}
\alias{dic_asignar_tipos}
\title{Asignar tipos de columna según diccionario}
\usage{
dic_asignar_tipos(
  datos,
  diccionario = NULL,
  path_diccionario = "diccionario.xlsx",
  sheet_diccionario = 1,
  idioma = NULL,
  modulo = NULL
)
}
\arguments{
\item{datos}{\code{data.frame} con nombres ya iguales a \code{diccionario$variable}.}

\item{diccionario}{\code{NULL} (se carga de \code{path_diccionario}/\code{sheet_diccionario}),
un \code{data.frame} o una ruta a archivo \code{.xlsx}/\code{.xls}/\code{.csv}.}

\item{path_diccionario}{Ruta al diccionario cuando \code{diccionario = NULL}
(defecto \code{"diccionario.xlsx"}).}

\item{sheet_diccionario}{Hoja si es Excel (nombre o índice). Por defecto \code{1}.}

\item{idioma}{(opcional) valor para filtrar \code{diccionario$idioma} si existe.}

\item{modulo}{(opcional) valor para filtrar \code{diccionario$modulo} si existe.}
}
\value{
\code{data.frame} con clases convertidas.
}
\description{
Convierte las clases de las columnas de \code{datos} usando un diccionario que,
como mínimo, debe incluir \code{variable} (nombre final de la variable) y \code{tipo}.
Opcionalmente puede incluir \code{niveles} para factores (p. ej. \code{"A,B"} o
\code{"1=No,2=Sí"}), y columnas \code{idioma}/\code{modulo} para filtrar.
}
\details{
Reglas:
\itemize{
\item \code{"date"}      -> \strong{no} se modifica
\item \code{"character"} -> \code{as.character()}
\item \code{"numerical"} -> \code{as.numeric()} (también acepta \code{"numeric"}/\code{"double"})
\item \code{"logical"}   -> \code{as.logical()}
\item \code{"factor"}    -> \code{factor()}; si \code{niveles} existe:
\itemize{
\item \code{"A,B"} o \code{"A|B|C"} fija niveles
\item \code{"1=No,2=Sí"} mapea códigos a etiquetas
Si \code{niveles} no casa con los datos, se cae a \code{as.factor()} y se avisa.
}
}

Si la conversión introduce nuevos \code{NA}, se emite un \code{warning}.
}
