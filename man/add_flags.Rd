% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8-add-flags.R
\name{add_flags}
\alias{add_flags}
\title{Añadir flags de presencia y resumen longitudinal}
\usage{
add_flags(pre, long, post = NULL, summary_prefix = "long_")
}
\arguments{
\item{pre}{data.frame de PRE (o \code{NULL} si no aplica). Requiere columna \code{email}.}

\item{long}{data.frame de LONG (o \code{NULL} si no aplica). Requiere \code{email} y \code{date}.}

\item{post}{data.frame de POST (o \code{NULL} si no aplica). Requiere \code{email}.}

\item{summary_prefix}{Prefijo a aplicar a las columnas agregadas de \code{long}
antes de unir con \code{pre} y \code{post}. Por defecto \code{"long_"}.
Los nombres objetivos son \code{paste0(summary_prefix, c("n","first_date","last_date"))}.
Si alguno ya existe en el destino, se crea una variante única (p.ej. \code{"long_n.1"}).}
}
\value{
Lista con tres data frames: \code{pre}, \code{long}, \code{post} (este último \code{NULL} si no se pasa).
}
\description{
Añade indicadores cruzados de presencia entre módulos
y un resumen por persona (métricas agregadas de LONG por email)
}
\details{
\strong{Flags cruzados:}
\itemize{
\item En \code{pre}:  \code{flag_long}, \code{flag_post}.
\item En \code{long}: \code{flag_pre},  \code{flag_post}.
\item En \code{post}: \code{flag_pre},  \code{flag_long}.
}

\strong{Resumen LONG por persona (agregado por \code{email}):}
\itemize{
\item \code{n_mediciones}: número de filas en LONG por persona.
\item \code{primera_fecha}: primera fecha observada en LONG.
\item \code{ultima_fecha}: última fecha observada en LONG.
\item Idempotencia: puedes llamar a \code{add_flags()} varias veces sin que se creen columnas duplicadas
por el \code{left_join()}, porque las columnas agregadas de \code{long} se renombran con prefijo
y, en caso de colisión, se desambiguan con \code{make.unique()}.
\item Flags recalculados: \code{flag_pre}, \code{flag_long}, \code{flag_post} se recalculan en cada llamada
y sobrescriben si ya existen.
\item Tipos: \code{date} en \code{long} debe ser \code{Date} o coercible a \code{Date}.
}
}
\examples{
# res <- add_flags(pre, long)
# res$pre; res$long

}
