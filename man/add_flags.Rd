% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-flags.R
\name{add_flags}
\alias{add_flags}
\title{Añadir flags entre PRE, LONG y POST (a prueba de colisiones)}
\usage{
add_flags(pre, long, post = NULL, summary_prefix = "long_")
}
\arguments{
\item{pre}{Tibble con una fila por participante (fase PRE). Requiere columna \code{email}.}

\item{long}{Tibble con varias filas por participante (fase LONG). Requiere \code{email} y \code{date}.}

\item{post}{Tibble opcional con una fila por participante (fase POST). Requiere \code{email}.}

\item{summary_prefix}{Prefijo a aplicar a las columnas agregadas de \code{long}
antes de unir con \code{pre} y \code{post}. Por defecto \code{"long_"}.
Los nombres objetivos son \code{paste0(summary_prefix, c("n","first_date","last_date"))}.
Si alguno ya existe en el destino, se crea una variante única (p.ej. \code{"long_n.1"}).}
}
\value{
Lista con tres tibbles: \code{pre}, \code{long}, \code{post} (este último \code{NULL} si no se pasa).
}
\description{
Enrichce tibbles de PRE/LONG/POST con flags de presencia cruzada y
métricas agregadas de LONG por email, evitando colisiones de nombre en joins.
}
\details{
\itemize{
\item Idempotencia: puedes llamar a \code{add_flags()} varias veces sin que se creen columnas duplicadas
por el \code{left_join()}, porque las columnas agregadas de \code{long} se renombran con prefijo
y, en caso de colisión, se desambigüan con \code{make.unique()}.
\item Flags recalculados: \code{flag_pre}, \code{flag_long}, \code{flag_post} se recalculan en cada llamada
y sobrescriben si ya existen.
\item Tipos: \code{date} en \code{long} debe ser \code{Date} o coercible a \code{Date}.
}
}
\examples{
# res <- add_flags(pre, long)
# res$pre; res$long

}
