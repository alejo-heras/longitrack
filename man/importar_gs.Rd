% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/google-sheet.R
\name{importar_gs}
\alias{importar_gs}
\title{Importar (y opcionalmente mapear) datos desde Google Sheets}
\usage{
importar_gs(
  idioma = c("es", "en", "both"),
  modulo = c("pre", "long", "both"),
  urls,
  hoja = "Respuestas de formulario 1",
  path_diccionario = "diccionario.xlsx",
  sheet_diccionario = 1,
  add_idioma = TRUE,
  mapping = TRUE,
  mapping_verbose = TRUE
)
}
\arguments{
\item{idioma}{"es", "en" o "both".}

\item{modulo}{"pre", "long" o "both".}

\item{urls}{lista con estructura:
list(es = list(pre=..., long=...), en = list(pre=..., long=...))}

\item{hoja}{nombre de la hoja a leer (por defecto "Respuestas de formulario 1").}

\item{path_diccionario}{ruta al Excel del diccionario.
Por defecto "diccionario.xlsx" en el directorio de trabajo actual,
salvo que se especifique lo contrario}

\item{sheet_diccionario}{Nombre (character) o índice (numeric) de la hoja del
Excel del diccionario. Por defecto 1. Si no existe, se informa el listado
de hojas disponibles.}

\item{add_idioma}{si TRUE añade columna \code{idioma} a los datos leídos.}

\item{mapping}{si FALSE, no se aplica mapeo (por defecto TRUE).}

\item{mapping_verbose}{si TRUE imprime los resúmenes de mapeo (por defecto TRUE).}
}
\value{
\itemize{
\item Si \code{modulo = "both"}:
\verb{list(pre = list(datos=<df>, mapeos=<lista_por_idioma>), long = list(datos=<df>, mapeos=<lista_por_idioma>))}
\item Si \code{modulo != "both"}:
\verb{list(datos=<df>, mapeos=<lista_por_idioma>)}
}
}
\description{
Lee \code{pre} o \code{long} (o ambos) para uno o dos idiomas y, por defecto,
aplica el diccionario \code{diccionario.xlsx} de la raíz del proyecto con
\code{renombrar_con_diccionario()}.
}
